name: 'dbt_production_framework'
version: '1.0.0'
config-version: 2

profile: 'dbt_production_framework'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_production_framework:
    staging:
      +materialized: view
    warehouse:
      +materialized: table
